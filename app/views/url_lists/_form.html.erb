<%= form_with(model: url_list, local: true) do |form| %>
  <% if url_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(url_list.errors.count, "error") %> prohibited this url_list from being saved:</h2>

      <ul>
      <% url_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :full_url %>
    <%= form.text_field :full_url %>
  </div>

  <div class="field">
    <%= form.label :short_url %>
    <%= form.text_field :short_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
